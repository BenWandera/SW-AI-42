services:
  - type: web
    name: SW-AI-42
    env: docker
    dockerfilePath: ./api/Dockerfile
    dockerContext: ./api
    plan: free
    autoDeploy: true
    buildCommand: echo "Building with Git LFS support"
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: PORT
        value: 8000
